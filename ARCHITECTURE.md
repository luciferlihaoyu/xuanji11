# 个人 AI 平台 · 架构总览（ARCHITECTURE）

> 本文件是北斗 / 璇玑 / 天宫三个自建应用共享的**架构事实源**。
> 完整规划见《AI 工具统合分析与规划》，逐任务落地见《详细执行计划》。
> 更新：2026-08-27（碧霄）

## 一、目标

把分散的 AI 工具整合为一个**统一、可热插拔、易维护、普通人可用**的个人平台：
统一入口、统一认证、统一模型网关、统一存储；外部能力以插件形式按需接入。

## 二、组成

### 自建应用（各自内置数据库，单容器自包含）
- **北斗**：网文创作工作台（React 18 + FastAPI + SQLite）。
- **璇玑**：知识 / 工作流平台（React 19 + Hono/tRPC + Drizzle + 向量检索）。
- **天宫**：多智能体 DAG 编排（同璇玑技术栈 + 预算/熔断 + 可靠消息总线 + MCP 外部执行器）。

### 外部组件
- **天枢 / new-api**：LLM 统一网关（聚合各家订阅模型）。
- **AList**：多云盘存储与备份。
- **OpenClaw / Kimi / DSH**：外部 Agent 与助手。
- **本地 Ollama**（规划中）：本地算力，注册进天枢。

## 三、目标架构

- **天宫 = 统一主平台**：门户、看板、插件中心、认证中心均落在天宫；北斗/璇玑为被天宫统管的独立服务。
- **MCP = 可热插拔插件总线**：stdio + Streamable HTTP，`tools/list` 动态发现；外部能力封装为 MCP Server，按需启停，不硬编码。
- **天枢 = 统一模型网关**：云端订阅模型 + 本地 Ollama 均注册为渠道，按策略路由。
- **AList = 统一存储/备份**：各应用数据目录打包归档至此。

```
统一域名（反代）
   ├── /        → 天宫（主平台：看板/门户/插件中心/认证）
   ├── /beidou/ → 北斗（独立服务，天宫统管）
   └── /xuanji/ → 璇玑（独立服务，天宫统管）
天宫 ←MCP→ 插件：AList / 天枢 / 璇玑检索 / Ollama / ops
天枢 ← 云端订阅模型 + 本地 Ollama
AList ← 三应用数据目录备份
```

## 四、集成原则

1. 各服务**独立部署、自包含**（数据库随应用，整合后为单容器）。
2. 能力通过 **MCP 暴露**，热插拔、可发现，不硬编码依赖。
3. **统一认证**（天宫为认证中心）、**统一域名入口**。
4. **数据备份 = 打包各应用数据目录 → AList**（整合后无需单独 dump 数据库）。

## 五、阶段路线（摘要）

P0 现状固化与安全 → P1 天宫主平台化 → P2 插件化（MCP）→ P3 本地算力 → P4 体验与自动化。

## 六、相关文档

- 完整规划报告：`/115/方案整合/AI工具统合分析与规划.md`
- 详细执行计划：`/115/方案整合/详细执行计划.md`
